NEXT_PUBLIC_SITE_URL=https://ovrld.co
NEXT_PUBLIC_ALLOW_INDEXING=false
# Set OVRLD_STATIC_EXPORT=1 only for static review hosting.
# Keep mailing provider secrets server-side; the preview uses a non-storing mock adapter.
